schema:
  - './src/schema/index.gql'
generates:
  src/generated/codegen.ts:
    plugins:
      - add:
          content: '/* eslint-disable */'
          placement: 'prepend'
      - 'typescript'
      - 'typescript-resolvers'
    config:
      useIndexSignature: true
      contextType: 'ApolloContext'
hooks:
  afterAllFileWrite:
    - prettier --write
